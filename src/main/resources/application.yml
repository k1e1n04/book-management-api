spring:
  application:
    name: book-management-api
  datasource:
    url: jdbc:postgresql://localhost:5432/book_management
    username: app
    password: password
    driver-class-name: org.postgresql.Driver
  flyway:
    # jOOQのコード生成時にマイグレーションは完了しているため無効化
    enabled: false
